对于并发型代码, 经验总结:
这类代码主要分为两个方面的技术问题:
- 生命周期管理
- 并发正确性

所以可以看到, 我的单元测试基本上也是围绕这两个方向完成.(test_frameserver_v2/v3.py)

在Python中, 生命周期管理大部分时候是隐式进行的, 但当开始需要用Buffer对象, ctypes导出指针, NDArray映射Buffer对象的时候, 则特别需要注意管理这些映射到已有内存的对象的有效性.
如果我们关注close方法, 就会注意到, 例如ctypes对象导出的Buffer指针, 需要在Buffer关闭前显式删除, 否则Python会提示错误, 资源无法完全关闭.
NDArray似乎在del时会自行删除导出指针, 但是依然保持显式的导出删除是更好的做法.

生命周期管理部分还包括在__init__时的显式对象类型声明. Cpp推荐使用RAII的模式. 在不涉及外部通信的操纵的时候, 我认为实例化即资源获取是合理的. 我的RingBuffer和FrameServer也是这么做的.
这么做有多个好处:
- 在init失败的时候(例如系统资源不足), 对象会直接被python解释器回收, 这意味着外部不需要样板代码判断操作是否成功, 不成功则清理(内部finally或者__del__方法). 这将复杂度内聚在了内部(内部执行清理), 对外直接raise (Let it crash)
- 在需要元编程操作inspect的时候, init后就存在的对象能更稳定的被扫描到, 而动态对象通常难以被正确捕捉. 参见MpObjProxy的扫描过程.

当然, 涉及到外部硬件操作的时候, RAII模式并不总是好的, 一方面可能会让init接口变得非常冗长, 另一方面你可能不希望对象关闭=销毁(就像mp.Process那样, 无法重复开启), 这个时候, init承担python语言层的声明, 有专门的open函数负责和外部硬件设备通信, 是更稳妥的选择.
我的几个硬件类基本都是这么做的.


对于并发正确性, 我认为核心的哲学是思考并发的可能性. 一般而言, 一个multiprocessing对象, 可以被多个进程同时调用的级别是函数/方法级别. 因而需要思考验证函数数n^2的可能性.
实际验证中, 你的多个方法最好剥离不同的数据依赖层. 这样仅需要思考如何处理含有操作相同数据依赖的对象之间的并发.
最简单的处理并发的方式是加锁避免Race Condition. 并发就是在安全和效率之间做权衡, 为了保证安全性的前提下做出最大效率. 我认为:
- 对于加锁保护的情形: 
  - 小锁保护频繁读写的少量数据间的一致性, 大锁保护非频繁读写的数据. 
  - 锁的获取遵循从大到小, 以避免ABBA死锁问题. 
  - 在能够不同时获取两把锁的情况下, 应该先后进出两个锁的临界区, 减少不必要的阻塞. 除非你**强要求**这两把锁保护的数据之间的一致性, 否则思考脏读的可能性.
  - 一些简单的性能数值参考: Python字节码单条指令通常在25ns左右, 一个线程锁t.lock.acquire()/release()大约50ns, 进程锁(操作系统管理)大约是其5倍 250ns. Condition包装比Lock多不了多少, 多个位数ns. 更多请参考 mptest_mp_mt.py
  - 关于等待: 等待数据准备好是常见场景, condition.wait_for可以在单重condition等待的时候使用, 若涉及到多condition, 则存在常见陷阱:
    等待条件1准备好后, 等待条件2, 条件2准备好后, 根据你的代码阻止: 若在等待条件2时, 长久持有条件1的锁, 这显然是非常不合理地减少并发吞吐了; 如果等待条件2时没有持有条件1的锁, 则条件1状态可能已经改变, 根据改变方向, 有可能仍然满足后续执行的条件, 或是不满足, 则需要重新等待.
    这个时候考虑引入乐观/悲观条件, 优先等待悲观条件, 等待完毕后检查乐观条件, 如果乐观条件不满足, 则继续等待下一个悲观条件时机. 此处则需要根据具体情况分析 参考frameserver_v2/v3.get_sync对slot和data两个availablity的等待.

- 关于非锁的情形, 你需要思考你是否能接收TOCTOU问题, 因为非锁只能原子读取一个状态, 总是没办法保证状态间一致性的. 总结下来可能就是读写分离和利用cpu机器特性完成原子读
  - 读写分离的常见场景是读多写少(或读分散, 写集中), 为了避免每次读都阻塞写, 可以实现顺序锁, 但在python字节码的层面上, 顺序锁性能一定差(参见系统锁的获取/释放耗时量级), 所以不考虑, 宁愿降低锁粒度.
  - 另一种情况就是考虑原子读, uint64在64位机器上的执行是原子的, 所以数据内部不会撕裂, 但是仍需要考虑TOCTOU问题. 这也就是一种脏读技巧.
  - 原子读写在cpp层面用atomic来完成, 配合cpp的内存模型, 建立适当的内存屏障, 可以实现无锁读写并发(标志位检查数据可用性), 但是一定避免做过多的提前优化, 可能并发性能瓶颈根本不在锁上.


